spring.application.name=payments

# Phase 1: Postgres that (compose.yaml o goc repo, host port 5433).
# H2 khong mo phong dung hanh vi khoa -> khong dung de hoc isolation duoc.
spring.datasource.url=jdbc:postgresql://localhost:5433/payments
spring.datasource.username=payments
spring.datasource.password=payments

spring.jpa.hibernate.ddl-auto=create-drop

# Bat len de NHIN THAY tung cau SQL Hibernate gui di.
spring.jpa.show-sql=true
